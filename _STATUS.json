{
  "focus": "S10 shipped both P2 fixes to origin/main and groomed PLG-4 to empty. (1) fix-anthropic-direct-task-serialization: the in-process `relay` MCP server (circular SDK object) was passed to the Messages API mcp_servers field → JSON.stringify crash on every anthropic-direct task; added mcpServersToAnthropicConnectors projection (remote URL connectors only, mirrors openai-direct), regression test + real dev-server task smoke → result 'ok'. (2) fix-inbox-checkpoint-realtime: Inbox list + unread badge now subscribe to the existing pending-approvals SSE as an invalidation trigger (re-pull /api/notifications on each snapshot), 741ms insert→emit smoke vs prior ~15s poll. PLG-4 groomed: free-key tier DEFERRED (brand-timing + Website-issuer dep, held), founding-supporter DROPPED → no live growth loop queued. All pushed (705d095a/1b10404e/891e759b); no open loose ends.",
  "blockers": [],
  "next": [
    "PLG-4 has NO live growth-loop candidate: free-key tier DEFERRED (re-open only when operator resurfaces it AND Website issuer can participate), founding-supporter DROPPED, reverse trial DEAD. Next session's PLG work is reactive-only until operator re-opens free-key.",
    "Reactive: Website owes the T-30 renewal email (later-9/later-10 open); held #5/#6/#11/#12 awaiting customer retest (labeled bug + awaiting-retest, no reply)",
    "Standing: flag each new pack changelog: line + any docs/trust/* URL move on _RELAY",
    "Backlog (pre-existing): chore-deprecated-transitive-deps (P3) · npm 2FA-hardening now OIDC works · stale pdfjs-dist in serverExternalPackages",
    "Optional cleanup (operator call): orphaned ~/.stagent-*/~/.ainative-* isolated-instance data dirs on disk — harmless but stale; some may be RELAY_DATA_DIR targets, so confirm before rm"
  ],
  "recent": [
    "S10 (2026-07-02): shipped both P2 fixes + groomed PLG-4 to empty. fix-anthropic-direct-task-serialization — root cause: withAnthropicDirectMcpServers merges the in-process `relay` server (createToolServer().asMcpServer(), a circular SDK object with a `root` back-ref, minified 'HJ') into params.mcp_servers verbatim; the Messages API mcp_servers is for REMOTE URL connectors and the SDK JSON.stringifies the body → crash before the model call on EVERY task. Relay tools already served locally via forProvider('anthropic')+executeHandler, so injection was crashing AND redundant. Fix: mcpServersToAnthropicConnectors projects the merge to the URL-connector shape (skips relay + local stdio), mirrors openai-direct's mcpServersToOpenAiTools. Regression test builds the exact cycle; real dev-server anthropic-direct task now completes (result 'ok'). fix-inbox-checkpoint-realtime — InboxList+UnreadBadge polled 10s, never subscribed to a stream, so HITL checkpoints surfaced ~10-15s late; both now subscribe to the existing /api/notifications/pending-approvals/stream (~750ms tail, carries WorkflowCheckpoint rows) as an invalidation trigger and re-pull the authoritative list — /api/notifications stays the single render source, poll kept as fallback. 741ms insert→emit smoke. 51/51 runtime+notifications tests pass. Commits 705d095a/1b10404e/891e759b. Memory: anthropic-direct-mcp-servers-remote-only",
    "S9 sweep (2026-07-02): stagent→ainative→relay legacy-symbol audit (2540 ainative + 250 stagent hits, categorized). Fixed 3 LIVE divergence bugs where tooling/boot-migration read the orphaned ~/.ainative DB while the app reads ~/.relay: (1) migrateLegacyData() was a one-hop stagent→ainative migrator stranding data at a dir the app ignores — now a chained MIGRATION_CHAIN ~/.stagent→~/.ainative→~/.relay; (2)(3) drizzle.config.ts + backfill-row-key-normalization.ts repointed AINATIVE_DATA_DIR||~/.ainative → RELAY_DATA_DIR||~/.relay + relay.db, matching env.ts 'RELAY_* only' policy. Fixed stale governance docs (dead Developer/ainative path → orionfold/relay, brand prose, ~/.ainative comments). Kept load-bearing legacy strings (shim internals, enum values, $AINATIVE_DATA_DIR manifest placeholder, live ainative-business npm pkg, getAinative* delegators). 16 migration tests green, tsc clean, dev boot smoke HTTP 200 against real ~/.relay/relay.db. Commit d6693d55. Memory: legacy-rebrand-divergence-bugs",
    "S9 ship (2026-07-02): 0.22.1 published (OIDC, SBOM, GitHub Release v0.22.1) — cleared the S8 unreleased fix bundle. Followed the proven --no-git-tag-version + manual release-commit recipe (npm version refuses a staged tree); PATCH bump so apiVersion window untouched. Pushed 5 S8 local commits + release commit 2e0ab3bd + tag. Publish workflow green, npm shows 0.22.1",
    "S8 ship (2026-07-02): cost-trust P1 bundle, 3 fixes TDD'd + live-smoked on a real dev server, committed LOCAL: (1) rail SPEND tiles read new meteredSpend/planPricedMonthlyMicros snapshot fields — the $20.00 'cost' was the guardrails' plan-price budget basis, substitution kept for budgets, display repointed; (2) resolvePreferredModel (pin > preference tier > quality default) wired into claude-code execute/resume (SDK got NO model before → silent Opus), aux calls, and both direct runtimes — balanced → claude-sonnet-4-6 proven; (3) sendOllamaMessage metered + provider ollama prices as $0 local-free. Found + spec'd fix-anthropic-direct-task-serialization (P2, pre-existing circular-JSON on every anthropic-direct task run)",
    "S8 groom (2026-07-02): open issues triaged — held bugs #5/#6/#11/#12 labeled bug + new awaiting-retest label (no customer reply to 0.16.0 retest asks; no fix specs until retest evidence); shipped announcement issues #14–#19 closed per #2/#3/#10 convention; feat-prepublish-tarball-smoke exposed as a phantom backlog item and dropped"
  ],
  "metrics": {
    "released": "0.22.1",
    "premium_packs": 1,
    "paid_updates_delivered": 1,
    "sbom_components": "491 prod deps (CycloneDX 1.5, measured 2026-07-01)",
    "npm_tarball": "1.5 MB (0.21.0 publish log)",
    "prod_artifact": "~35.5 MB gz (0.16.0 measurement, not re-read)"
  }
}
