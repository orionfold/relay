# dependencies
node_modules/

# build output
.next/
dist/

# environment
.env
.env.local
.env.*.local
.relay-dev-data/

# OS
.DS_Store

# IDE
.vscode/
.idea/
.obsidian/

# next.js
next-env.d.ts

# build artifacts
tsconfig.tsbuildinfo

# test coverage
coverage/
.playwright-mcp/

# project management (local only)
ideas/
methods/
output/
.archive/handoff/
/HANDOFF.md
/CODEX-CC.md
/OPERATOR-REQUIREMENTS.md

# strategy channel symlink — planning artifacts live in the private
# orionfold/strategy repo (_IDEAS -> ../strategy/relay/_IDEAS); never ship here
_IDEAS

# strategy channel symlink — dev-time spec/backlog lives in the private
# orionfold/strategy repo (_SPECS -> ../strategy/relay/_SPECS); never ship here
_SPECS

# strategy channel symlink — product-doc asset corpus lives in the private
# orionfold/strategy repo (_ASSETS -> ../strategy/relay/_ASSETS); never ship here
_ASSETS

# strategy co-development flows live in the private strategy repo
# (_FLOWS -> ../strategy/relay/_FLOWS); never ship here
_FLOWS

# Claude Code self-improvement reference docs (local only — playbook + Opus 4.8
# best-practices standard; see _REFER/cc-self-improve-all-projects.md)
_REFER/

# local package artifacts
ainative-*.tgz
orionfold-relay-*.tgz
dist-artifacts/
supabase/.temp/

# superpowers skill scratch output (local only)
.superpowers/

# refresh-content-pipeline state (local only)
.refresh-pipeline/

# private operational docs (local only)
PRIVATE-INSTANCES.md
SCRATCH.md

# operator-only workflow policies (local only — never ship; outranks .claude/skills)
CLAUDE.local.md

# runtime lock files
.claude/scheduled_tasks.lock

# Secret sauce — dev-time CC/Codex steering, held back for the paid model.
# NOT product runtime deps (the SDK reads the end-user's cwd, not this repo).
# Kept local on disk; only un-tracked from git. KEEP .claude/apps/starters/
# TRACKED — that is product seed data the homepage renders.
/CLAUDE.md
/AGENTS.md
/MEMORY.md
/FLOW.md
.claude/skills/
.claude/plans/
.claude/reference/
.claude/agents/
.claude/commands/
.claude/rules/
.claude/hooks/
.claude/handoff-profile.yaml

# AI Native Business book — authoring + content extracted to a private peer
# repository (2026-06). The book stays out of the public Relay repository and
# npm packaging; npm `files` never included these. Kept local on
# disk; only un-tracked from git. (.claude/skills/book-updater
# is already covered by the .claude/skills/ rule above.)
/book/
/ai-native-notes/

# Retired User Guide / generated-doc corpus. The authoritative customer-facing
# asset and guide source is strategy/relay/_ASSETS; these ignores prevent old
# generators from silently recreating the root corpus. The in-app User Guide UI
# and generated docs stopped shipping in 2026-06. Session-local plans/specs are
# internal continuity records.
# Keep hand-written contributor documentation such as docs/plugin-security.md
# tracked, but never publish the superpowers scratch history.
/docs/superpowers/plans/
/docs/superpowers/specs/
/docs/features/
/docs/journeys/
/docs/use-cases/
/docs/index.md
/docs/getting-started.md
/docs/why-ainative.md
/docs/manifest.json
/docs/.coverage-gaps.json
/docs/.last-generated
