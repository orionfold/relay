# Relay — HANDOFF

_Last updated: 2026-07-10 (pt: **Demo UPDATE #2 DELIVERED + `_RELAY` #27 sent — awaiting operator strategy-commit, then website re-copy.** Operator walked the published demo and requested: (1) **remove the dead-menu FADE entirely** — `grayOutDeadMenus()` + `data-relay-demo-dead` tag GONE; un-emitted links still neutralize to `#` (no 404s) but look normal; verifier now asserts the OPPOSITE (fails if any link is faded). Also `unfadeDisabledControls()` clears captured `disabled` buttons (Activate/Pull/Save target) — no faded cards/buttons on any route. (2) **Replace the two synthetic apps** Launch Ops + Agency Ops with **REAL packs** — the demo now installs `relay-marketing` + `relay-agency-pro` (via built CLI `pack add` + prod-signed license fixture, in `capture-relay-demo.mjs::installRealPacks()` after seed; paid packs are license-gated, no dev bypass). **Web Designer kept synthetic** (drives Web-Publish lane). "Demo " name prefix stripped. (3) **New `/presets` captured screen** (Compose→Presets agent-preset gallery; one line in BREADTH_ROUTES, build auto-discovers). (4) Demo now presents **LICENSED** ("Licensed to manav@orionfold.com") per operator. Gate GREEN (25 routes, 4/4 lanes, 0 leaks; 33 unit tests). `_RELAY` #27 (Relay→Website, `[open]`) sent: re-copy the SAME two dirs (`dist/relay/demo/` + `dist/brand/`) verbatim — pickup mechanics unchanged from #25, only bundle contents changed. **Operator commits the strategy repo BEFORE telling website to pick up #27.** All on `main`; NO git run in strategy repo. Memory [[assets-demo-option-c-viability]] (no-fade+real-packs+presets update appended). Prior detail (demo #25 re-export, API docs Ch 01/02, theme switcher, T5/T3/T2/T1) in git. **NEXT SESSION: demo done pending operator commit + website #27 reply; API docs Ch 02+ still per-chapter loop. See START HERE.**)_

## ▶️ NEXT SESSION — **START HERE: API docs = per-chapter publish loop; Ch 02 GATED on website's #23 reply** (cadence changed by operator 2026-07-10)

- **⭐ P0 THIS-SESSION START — API docs: per-chapter publish, currently BLOCKED on the website's `_RELAY` #23 reply.** Operator changed the cadence (2026-07-10): publish API chapters ONE AT A TIME, get render/pipeline issues back, fix at source, THEN author the next — NOT "author all 8, flag green once." **#23 (Relay→Website) is sent and `[open]`; the website has Ch 01 to land + must reply.** First action next session: **check `orionfold-website/_RELAY.md` for the #23 reply.** If it reports issues → fix Ch 01 at source (`_ASSETS/api/reference/01-overview-local-api.md`), re-run the gate, send a follow-up `_RELAY`. If it landed clean → author Ch 02 (24 core-records routes) using the proven loop below, then send its own single-chapter publish `_RELAY`. Do NOT batch 02-08. Remaining after 01: 02(24 core records)/03(31 agents-chat-runtimes)/04(27 workflows)/05(32 packs-apps-tables)/06(31 settings-license-instance)/07(15 files-observability)/08(6 webhooks) = **166 routes**. Per-chapter authoring loop (proven on Ch 01):
  1. **Fan-out recon:** parallel Explore over the chapter's `route.ts` set → source-traced request/response/errors/side-effects (follow Zod schemas, `NextResponse.json`, DB enums; NO invented shapes — `rules.noInventedShapes:true`).
  2. **Author on Opus** into `reference/<NN>-….md`: real shapes + worked `curl` examples + a `![alt](relay-shot:<id>)` marker (pick a fitting shot id from the 23 in `screenshots/metadata/manifest.json`) + NO body H1 (frontmatter `title` is SSOT). Put the un-inventable details (no-try/catch → unhandled rejection, counts that differ from inputs) in "Do Not Depend On".
  3. **Flip `reviewed:true`** for those endpoints in `api-tracker.json` + fill their `summary`/`request`/`response`/`errors` one-liners (a scratchpad `.mjs` `Object.assign` loop is cleanest — avoid inline `node -e` with `~` in strings, the shell mangles it).
  4. **Gate:** `node _ASSETS/api/scripts/verify-api-docs.mjs --require-files` — the chapter is clean when it has 0 failures/warnings (every endpoint path mentioned). It exits 1 until ALL 8 pass (02-08 stubs still fail by design). Spot-check literal error strings against source verbatim before trusting the draft.
  - **Per-chapter publish (NEW cadence 2026-07-10):** send a single-chapter `_RELAY` (Relay→Website) as each chapter goes gate-clean; website lands it like docs → `src/content/relay-api/` → `/relay/api/`, footer-CTA-only, and replies with render/pipeline issues. Fix at source, then proceed to the next chapter. (Supersedes the old "re-flag green ONLY after ALL 8 pass" rule — that batched the risk; per-chapter surfaces pipeline issues early.)
  - **DONE 2026-07-10 (do NOT redo):** verifier hardened (fail-closed on `PLACEHOLDER_PROSE` + body-H1 + missing frontmatter-title); Ch 01 authored + gate-clean + **3 request-body tables de-indented to col 0** (were bullet-indented → collapsed as raw text in Astro/GFM); `_RELAY` #23 sent to publish Ch 01. Style guide `_ASSETS/api/style-guide.md`: NO em-dash, no "leverage/robust/…". Constraints: `_ASSETS`→strategy symlink (operator owns strategy-side git; NO push needed for the website to SEE a `_RELAY` edit — same-filesystem, [[strategy-repo-readwrite-only]]); NOT runtime-registry-adjacent (no `npm run dev` smoke). Memory [[api-docs-prose-gate-and-per-route-review]], [[assets-docs-claim-verify-beyond-catalog]], [[memos-number-trace-misses-structural-claims]]. **Markdown gotcha:** a table indented under a `-` bullet collapses to raw text in strict renderers (Astro) — author request tables at col 0.

- **⭐ P0 GATED — `_ASSETS` demo UPDATE #2 DELIVERED; `_RELAY` #27 sent; waiting on (a) operator strategy-commit, then (b) website re-copy + reply.** No-fade UI + real packs (relay-marketing + relay-agency-pro) + new `/presets` screen + licensed state, 25 routes gate-green on disk. **Operator's move:** commit the strategy repo (`_ASSETS/demo` + `_ASSETS/seed` M/D + untracked `dist/`/`source/captured/` deltas + `orionfold-website/_RELAY.md`), THEN point the website at `_RELAY` #27. Next session: check `strategy/orionfold-website/_RELAY.md` for the website's #27 reply (render issues: `/presets` route, the two real pack apps, licensed badge, anything keyed on the now-removed `data-relay-demo-dead` marker). If issues → fix at source, rebuild + re-verify (`node --preserve-symlinks --preserve-symlinks-main _ASSETS/demo/scripts/verify-relay-demo.mjs --base-path /relay/demo/ --dist _ASSETS/demo/dist`), send a follow-up `_RELAY`. **Open coherence Q for operator:** the License/Pack guided lane (402→Activate) now runs on a *licensed* instance — functions but reads slightly odd; adjust lane copy if it bothers. [[assets-demo-option-c-viability]] (no-fade+real-packs+presets update), [[assets-website-publish-contract]].

- **P0 NEXT — Web Publisher preview/gallery polish.** The page registry is built; remaining operator-guided feedback is to keep web-pack previews inside Relay chrome with a separate raw-view action, make blank gallery thumbnails meaningful or omit them, and normalize section-card click behavior.
- **P0 NEXT — `_ASSETS` golden-source marketing pipeline (multi-session).** Strategy-owned corpus via repo symlink. **⭐ Overriding guidance + verbatim operator origin instruction now live at the TOP of `_ASSETS/README.md`** — read it first; it outranks the pipeline data-flow order. The named quality bars are `~/orionfold/books/` (narrative/docs) and the Arena demo `~/ainative-business.github.io/` (static demo); the core requirement is derive-from-and-verify-against the live product (no drift, no fake). Runbook: `_ASSETS/flow/README.md` + `_ASSETS/README.md`. **Data-flow order (NOT priority order):** `features-catalog -> journeys -> seed -> screenshots -> api -> docs -> stats -> demo -> articles`. Full design: `~/.claude/plans/review-the-current-assets-expressive-bird.md`. Memory: [[assets-golden-source-workstream]], [[assets-supervisor-checks-existence-not-behavior]], [[assets-symlink-preserve-symlinks]], [[assets-website-publish-contract]].
  - **🌐 WEBSITE PUBLISH CONTRACT (NEW 2026-07-09, standing law) — `docs/superpowers/specs/2026-07-09-relay-assets-website-publish-contract-design.md` + pinned atop `_ASSETS/README.md`.** docs/api/demo/**memos** (renamed from `articles`, route `/relay/memos/`) are single-source, copy-verbatim **marketing/CTA surfaces** published to **orionfold.com** (`~/orionfold/website/` = PUBLISH TARGET; `ainative-business.github.io` = REFERENCE bar only, NOT a target). One-direction: Relay never writes the website, website never writes `_ASSETS`, Relay reads website read-only to prepare website-ready assets; fidelity enforced at source (vendored DS + drift-check, fails closed). Homes: demo→`public/relay/demo/` (Rail A bundle), memos/docs/api→content collections + `/relay/{memos,docs,api}/` (Rail B). Per-asset conversion (no stacking): **demo=sticky buy strip** (Relay builds into bundle, `live`-gated, never hardcode a live buy state), **memos=interstitial+footer**, **docs/api=footer only**. Website-side work requested via staged `_RELAY` handoffs (A=schemas+CTA hooks, B=per-asset publish), authored when the Relay gate is green, **operator-posted** to `orionfold-website/_RELAY.md` (never commit/push the strategy repo). **This spec shapes the target; it does NOT move asset production up the priority ladder** (enforcement → docs/demo false-greens → consolidation → memos). T1 enforcement + T2 docs + T3 consolidation + T5 memos (landed on website #18) + demo fan-out (4 lanes, LIVE #7) + theme-switcher (dark default, `_RELAY` #21) are now DONE; next along the ladder is API docs Ch 02-08 (Ch 01 + verifier done 2026-07-10) + `screengrabs/` cleanup (LIVE #8).
  - **🌐 LIVE publish handoff — `_RELAY` #15–#21 (2026-07-09/10), all on `orionfold-website/_RELAY.md`; operator commits/pushes the strategy repo (#21 is edit-only, uncommitted `M`, awaiting operator post).** State: **docs LANDED** (#16, unpushed). **Memos LANDED** (#18, `ad8f436` unpushed — `/relay/memos/` live w/ Draft chips + `noindex`; operator flips `status:published` → send website the 1-line filter change; website #20 asks a source-side SVG blank-line fix for the NEXT memo batch). **api — per-chapter publish STARTED** (#23 sent: land Ch 01 ONLY, report issues; supersedes #16's "all 8 then re-flag"; Ch 02 gated on the reply). **demo — theme switcher SHIPPED dark-default (#21);** bundle is 8-route gate-green + self-theming, verbatim copy; website's light-vs-dark **default** override is offered as a one-liner (their call). All entries newest-first at the TOP of that file (channel masthead says newest-first — the [[relay-channel-cross-peer-comms]] memo's "newest-at-bottom" is STALE for this channel).
  - **⚠️ "supervisor-green" ≠ Delivered.** The strict supervisor only checks file EXISTENCE, never product-truth — but the `assets-flow` gate now EXECUTES verifiers (T1 done). **Delivered** = built AND behaviorally verified; **Planned** = spec/stub only. Priority order now = **theme-switcher DONE (#21) + Demo fan-out DONE (4 lanes) + T5 memos DONE (#18) → ⭐ API docs per-chapter publish (Ch 01 sent #23; Ch 02 GATED on website reply — see START HERE at top) → T6 → `screengrabs/` cleanup LIVE #8**:
    | Deliverable | Status | Priority |
    |---|---|---|
    | **T1 Enforcement / Catalog drift-guard / T2 Docs (9 guides) / T3 Consolidation** — all EXECUTED by the `assets-flow` gate | **Delivered** | done (git) |
    | **Demo** — Option C, **all 4 `machines{}` lanes gate-green** + **24-route breadth re-captured** + operator polish (logo/CTA, coach removed); `_RELAY` #25 sent to publish. Landmines: [[assets-demo-hydration-wall]] · [[assets-demo-captured-controls-disabled]] | **Delivered (re-export #25)** | operator commit → website |
    | Seed / Screenshots (92 PNGs) / Stats / Journeys+coverage-matrix — materially in sync | Delivered | verify-not-rebuild |
    | **T5 Memos** — 5-memo "Relay Packs" series gate-green; **website LANDED (#18, `ad8f436` unpushed)**, drafts render w/ "Draft" chip + `noindex`; both ACKs confirmed. Only operator editorial review of the 5 drafts remains (flip `stage/status`→published → send website the 1-line filter flip). [[t5-memos-relay-packs-series]] | **Delivered** | **operator review of 5 drafts** |
    | **T6 Book** — compile 10–20 memos/articles (Spark→DGX path), joint operator+CC brainstorm (NOT autonomous) | Planned | deferred |
  - **▶️ API docs — per-chapter publish loop; Ch 01 sent for publish (`_RELAY` #23), Ch 02 GATED on the website reply → the NEXT-SESSION START-HERE item at the top of this file.** [[api-docs-prose-gate-and-per-route-review]].
  - **LIVE #8 — Consolidation tail (T3 mostly DONE 2026-07-09).** The 5 `assets-*` skills are authored, the marketing scripts + `.refresh-pipeline/` + `output/marketing-screenshots/` are gone, the hero shot is a capture target, the supervisor false-red is fixed ([[assets-t3-consolidation-built]]). **Remaining sweep:** (1) root `screengrabs/` (98 tracked PNGs, ~54MB) — DEFERRED because it's still referenced by live `.agents/skills/supervisor/SKILL.md` + `ainative-app/SKILL.md` + feature specs + seed data; removing it means editing those skill docs FIRST (a distinct blast radius). (2) orphaned `docs/features|journeys|use-cases|index.md|manifest.json` (keep `RELEASING.md`/`trust/`/`codex-browser-runbook.md`/`plugin-security.md`). (3) `npm run demo:refresh` + structural-fingerprint drift-guard.
  - **DEFERRED (operator, 2026-07-09) — claim-attribution gate for docs.** The per-claim accuracy pass (LIVE #10) is DONE; the operator explicitly deferred extending `verify-user-guides.mjs` toward claim-attribution (currently banned-phrase/section/word-count/image-existence only, so a future guide edit could reintroduce a wrong product-truth claim and still pass). Pick up when the docs corpus next changes (T3 consolidation did NOT touch the docs verifier).
  - **LIVE #9 — Book (T6) — JOINT OPERATOR+CC BRAINSTORM, not autonomous generation.** Action = open a brainstorming session with the operator; do NOT batch-generate. The T5 Relay Packs memo series (5 gate-green memos) is now candidate raw material for the compile. Gates lifted (docs + demo slice + memos all verified-green).
  - **🔨 Standing authorization (operator, 2026-07-09):** THROWAWAY-AND-REBUILD any part of `_ASSETS` to meet/exceed the quality/polish/taste bar. Overrides the "prefer small edits / preserve prior work" default for this corpus. Pinned in README origin block; memory [[assets-throwaway-and-rebuild-authorized]].
  - **Advisory:** catalog verify (gh-enabled) surfaces 5 shipped-issue-gap WARNINGS (Marketing line 0.33.0, funnel band 0.34.0, Web Publisher 0.35.0 not cited by version). Not blocking — a grooming signal for `features-catalog.md`.
- **P0 strategy-doc loose end:** `_SPECS/cards-design-system.md` exists through the `_SPECS` symlink as an untracked strategy-repo file (`relay/_SPECS/cards-design-system.md`). Do not commit/push the strategy repo unless the operator explicitly asks.
- **P0 Codex browser runbook is authoritative.** Use `docs/codex-browser-runbook.md`: in-app Browser/Browser plugin first for unauthenticated localhost/file/public pages; Codex Chrome extension for signed-in/profile state; Computer Use for GUI-only desktop flows; Chrome DevTools MCP only for isolated CDP debugging. Do not treat `open -a "Google Chrome" <url>` as sufficient verification.
- **P0 Codex handoff behavior:** `.codex/hooks.json` intentionally has no `Stop` handoff nudge. Use auto compact or operator-initiated `handoff`; do not re-enable the Stop hook unless asked.
- **P0 Codex frontend-skill behavior:** stale Stagent-era frontend design skills were removed from `.agents/skills` and `~/.codex/skills`. Do not reinstall them. Build any new Relay frontend skill later from current online research, Orionfold design-system docs, and project best practices.
- **Packs Publish:** READ trio + R5 + R4-mechanism are built/released in 0.35.0. R4 CUT remains deferred until the tarball size trigger and open decision #2 (most-installed boundary + Website coordination) are answered. R6/R7 community SEND loop stays last, behind TDR-039 substrate.
- **`pack-depth-next-wave` arc:** Video Creator still needs a clean synthetic reference model before build. Retail Investor remains open: value-heatmap + radar, prosumer-first ICP.
- **`_IDEAS/packs-robustify.md`:** R1+R3+R5 built. Next gate tranche: R2 install-time cross-pack collision check, then R7 integration/`joinKeys`, R4 provenanceOf API, R8 dependsOn when it earns weight.
- **Operator-guided feedback tasks (UNGROOMED, source `output/operator-walkthrough-feedback-2026-07-07.md`):** Web-pack preview/gallery polish remains open after the page-registry pass.

Standing candidates (LOW / reactive):
- Card-system follow-up: audit residual detail/table/dashboard cards that still use local raw-badge/status/action layouts, but the main card grids now use the shared footer.
- F5 follow-up: lift dense analytics dashboards `costs/cost-dashboard.tsx` + `apps/ledger-hero-panel.tsx` to the card recipe.
- Not-filed backlog `fix-pricing-bundled-stale-coldstart.md` + R2-4 `create_trigger` `appId` gap.
- #29 retry-with-backoff hardening; held-issue retests (#5/#6/#11/#12).
- Other staging R-runs (one R-run per session; harness ready).
- `chore-deprecated-transitive-deps` (P3); stale `pdfjs-dist` in `serverExternalPackages` (#10).

### Staging harness — ready, full loop proven end-to-end
- `relay-staging` · `staging-cli-run` · `staging-browser-smoke` · `staging-evaluate` are available. Driver-mix + headed-browser default live in the `staging-browser-smoke` skill.
- Re-run cadence: one R-run per session. `scripts/staging/browser-capture.mjs` is the headed PNG helper. `file://` mirror is per-build: `npm run build && node scripts/build-prebuilt-artifact.mjs` before verify.
- Constraints: work on `main`; `_SPECS`/`_IDEAS` edit-only; paid-frontier OK'd for agent steps.

## Known caveats

- **Codex desktop browser caveat:** use the in-app Browser/Browser plugin for ordinary localhost verification. Browser DOM snapshots may fail with an `incrementalAriaSnapshot` runtime mismatch; if so, stay in the in-app Browser and use targeted page evaluation/screenshots per the browser skill rather than falling back to normal Chrome.
- **apiVersion window:** bump `CURRENT_PLUGIN_API_VERSION` + previous-MINOR literal + 3 plugin example YAMLs only on a MINOR release. PATCH does not bump it.
- **The npx prod smoke (`scripts/npx-prod-smoke.mjs`) encodes API contracts that bundled fixes may change** and runs only at release. Before tagging, grep it for endpoints a bundled fix touches.
- **Pack `changelog:` map feeds recap surfaces.** Add a line with every pack version bump; template tests require it for Agency Pro.
- **Pack `price` is `string | {list, intro?, note?}`** behind `packPrice()`; render sites never branch on raw shape.
- **Automation blueprint vars MUST be auto-fillable.** Row-insert required vars need a same-named column or `{{row.<col>}}` default; scheduled blueprint required vars need defaults because app schedules dispatch with no human-supplied variables. `tables.listRows().data` is JSON string; parse before feeding `buildVariables`.
- **docs/index.md + docs/features|journeys|use-cases are gitignored** generated corpus. Public docs = README + SECURITY.md + docs/trust/ + docs/RELEASING.md + docs/plugin-security.md.
- **Pre-existing test failures (NOT regressions), 8:** `router.test.ts` (6), settings validator (1), settings/glance shadow-path (1); `src/__tests__/e2e/blueprint.test.ts` is environmental; `agency-pro-update.test.ts` can flake in full parallel but passes isolation.
- **`next` pinned exactly 16.2.4**; Next 16 emits `.next/node_modules` symlinks; artifact ships manifest + CLI relinks.
- **Blueprint/profile content must pass Zod schema**; registry skips invalid files with console.warn, causing "Blueprint not found" later.
- **Settings mobile overflow caveat:** 2026-07-08 card-system smoke found `/settings#settings-permissions` overflows at 390px because unrelated settings channel/provider rows and telemetry rail cells extend off-canvas. Preset cards were not the source. Track separately if mobile settings polish resumes.
- **`_ASSETS` seed reset caveat:** During the 2026-07-09 screenshot pass, rerunning `seed-relay-demo.mjs --reset-demo` on the already-used seeded DB hit a SQLite foreign-key constraint in `resetDemo()`. A non-reset seed upsert successfully applied the corrected settings and verifier stayed green. Prefer a fresh isolated data dir for future full reset tests, or fix reset deletion order before relying on reset against a reused DB.

## Anchors

- Strategy repo = read/write only unless explicitly instructed; commit/push/merge there only on operator request.
- **Two websites:** `~/orionfold/website/` (orionfold.com) = the PUBLISH TARGET for `_ASSETS`; `~/ainative-business.github.io/` = REFERENCE/quality bar only (never a publish target). Both read-only from Relay; Relay writes neither. See the publish contract atop `_ASSETS/README.md`.
- Work directly on `main`; no worktrees/branches unless operator asks.
- npm publishing uses OIDC via annotated `vX.Y.Z` tag and GitHub Actions. Every release attaches CycloneDX SBOM.
- Runtime-registry-adjacent changes require a real `npm run dev` smoke.
- gh issue/label writes are allowlisted.
- Check git history for prior art; verify field reports before fixing.

## Recently shipped / groomed

**Demo UPDATE #2 — no-fade UI + real packs + Presets + licensed, `_RELAY` #27 (2026-07-10, strategy-owned/uncommitted — operator commits):** Operator walked the published demo (post-#25) and asked for 4 changes, all folded at source, gate-green (25 routes, 4/4 lanes, 0 leaks; 33 unit tests). (1) **Removed the dead-menu FADE** — deleted `grayOutDeadMenus()` + the `data-relay-demo-dead` build-tag; un-emitted links still neutralize to `#` (no 404) but look normal; verifier flipped to assert the opposite (fails if faded). `unfadeDisabledControls()` clears captured `disabled` buttons (Activate/Pull/Save target) → zero faded cards/buttons. (2) **Two synthetic apps → REAL packs** — removed `demo-launch-ops`/`demo-agency-ops` from `_ASSETS/seed/data/fixture.json`; capture pipeline now installs `relay-marketing` + `relay-agency-pro` via built CLI `pack add` + prod-signed license fixture (paid packs license-gated, no dev bypass; `installRealPacks()` runs AFTER seed, NOT in the raw `.mjs` seed). **Web Designer kept synthetic** (Web-Publish lane). "Demo " name prefix stripped (now only Web Designer). (3) **New `/presets` route** (Compose→Presets agent-preset gallery; one line in BREADTH_ROUTES, build auto-discovers). (4) Demo now **LICENSED** (license stays → "Licensed to manav@orionfold.com"). Also re-ran `derive-fixtures.mjs` (separate manual step) to clear a stale removed-app activity-log line from the shim data. `_RELAY` #27 sent: re-copy SAME two dirs verbatim. All on `main`, no strategy-repo git by me. [[assets-demo-option-c-viability]] (update appended), [[assets-symlink-preserve-symlinks]], [[assets-website-publish-contract]].

**API docs → per-chapter publish cadence + Ch 01 table-fix + `_RELAY` #23 (2026-07-10, strategy-owned/untracked):** Operator changed the API-docs cadence to **publish one chapter at a time** (land → get render/pipeline issues back → fix at source → author next), replacing "author all 8 then re-flag green." Fixed Ch 01's 3 request-body tables (`context/batch`, `workspace/discover`, `workspace/import`): they were indented 2sp under a `- **Request** body:` bullet, which GFM/CommonMark/Astro parse as the bullet's paragraph text → the table collapsed to raw `|…|`; de-indented all to col 0, verifier stays Ch-01-clean. `relay-shot:home-cockpit` marker LEFT as-is per operator (website resolves it). Sent `_RELAY` #23 (Relay→Website, `[open]`) to land Ch 01 ONLY via the Docs idiom (`src/content/relay-api/` → `/relay/api/`, footer-CTA-only), asking for a reply with any render/pipeline issues before Ch 02. Ch 02 is now GATED on that reply. Also corrected [[strategy-repo-readwrite-only]]: the strategy repo is LOCAL to both website+Relay boxes → an uncommitted `_RELAY.md` edit is immediately visible cross-side; no push needed for visibility. [[api-docs-prose-gate-and-per-route-review]].

**API docs Ch 01 + verifier hardening (DELIVERED 2026-07-10, strategy-owned/untracked):** `verify-api-docs.mjs` now fails-closed on scaffold placeholder-prose (`PLACEHOLDER_PROSE`) + body-H1 (`stripFrontmatter` then reject `^# `) + missing frontmatter-title — was file-exists-only (a false-green). Chapter 01 (`command-palette/recent`, `context/batch`, `workspace/{context,discover,fix-data-dir,import}`) authored fully from source-traced `route.ts` via parallel Explore recon; `reviewed:true` flipped + tracker facts filled for the 6; `relay-shot:home-cockpit` marker, no body H1; literal error strings spot-checked verbatim; the un-inventable "no try/catch → unhandled rejection" details captured. 6/172 reviewed. [[api-docs-prose-gate-and-per-route-review]].

Older resolved session detail is durable in git + memory: demo re-export #25 (24-route + #24 fixes + logo/CTA + coach removed, superseded by update #2/#27 above; [[assets-demo-option-c-viability]]), theme-switcher (dark-default, `_RELAY` #21, [[demo-self-themes-from-bundled-css]]), Demo fan-out 4 lanes (Row Trigger + License/Pack, [[assets-demo-captured-controls-disabled]]), T5 tail (vendored `_design-system/`, [[relay-field-notes-pipeline-design]]), T5 memos (LANDED website #18, [[t5-memos-relay-packs-series]], [[memos-number-trace-misses-structural-claims]]), Demo Web Publish lane, T3 consolidation (`1c83e167`, [[assets-t3-consolidation-built]]), T2 Demo Option C (`dbf46722`/`36c498f6`, [[assets-demo-hydration-wall]]), T2 Docs claim-verify ([[assets-docs-claim-verify-beyond-catalog]]), T1 enforcement gate (`ea79474a`, [[assets-flow-executing-supervisor-built]]); and 2026-07-08 card-system pivots (`04aaeb6e`), 0.36.0 release, Web Publisher page registry, Packs Publish P1/R5/R4. Keep HANDOFF focused on live next actions.
