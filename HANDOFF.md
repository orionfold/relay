# Relay — HANDOFF

_Last updated: 2026-07-09 (pt: **T2 Demo Option C vertical slice — BUILT + behaviorally verified (gate GREEN). Executed all 8 plan tasks inline on Opus. Pipeline (all `_ASSETS/demo/`, gitignored → operator commits strategy-side): `derive-fixtures.mjs` (seed→API-shape fixtures, 4 tests) → `capture-relay-demo.mjs` (Playwright crawl of 5 routes against a seeded `next build && next start`) → `build-relay-demo.mjs` (stitch captured HTML + boot.js → dist) → `verify-relay-demo.mjs` (headless behavioral gate). `boot.js` (707 lines, 8 tests) = network+EventSource shim + Support-Triage state machine + guidance (ribbon/buy-strip/coach-callout/glow-guide) + Arena-style streaming ticker. `assets-flow` supervisor already wired (demo stage ok:true, both validators passed); corpus stays AMBER only on the seed validator (needs `--data-dir`, unrelated). Full cold e2e green; 12/12 unit tests. Screenshots in `output/demo-verify/`.**

    **⚠️ MAJOR DEVIATION from the plan (operator-approved mid-session, memory [[assets-demo-hydration-wall]]):** the plan assumed the interactivity gap was NETWORK. It is HYDRATION — Relay's `force-dynamic` App Router pages embed an RSC flight payload that needs a running `next start` server to `hydrateRoot`; served as flat static files (the GitHub-Pages target) React NEVER attaches, so captured buttons' native onClick is inert (verified: dev+prod capture, all chunks, zero page errors, with+without boot.js). Operator set target=GitHub Pages + objective="near-real app", so `boot.js` now OWNS interaction via DOM-driving (delegated click → mutate the real captured DOM). Structure stays 100% captured (can't drift — Option C's thesis intact); only click→state wiring is synthetic. Also switched capture from `next dev` → `next build && next start` (dev output throws "Unexpected token '<'" offline). NEXT: fan-out phase (other 3 machines, ~30 routes) OR operator review of the built slice; `deploy-relay-demo.mjs` still a stub. Prior: T2 Docs accuracy pass DELIVERED; T1 enforcement gate `ea79474a`.)_

## ▶️ NEXT SESSION — Post-release backlog

- **P0 NEXT — Web Publisher preview/gallery polish.** The page registry is built; remaining operator-guided feedback is to keep web-pack previews inside Relay chrome with a separate raw-view action, make blank gallery thumbnails meaningful or omit them, and normalize section-card click behavior.
- **P0 NEXT — `_ASSETS` golden-source marketing pipeline (multi-session).** Strategy-owned corpus via repo symlink. **⭐ Overriding guidance + verbatim operator origin instruction now live at the TOP of `_ASSETS/README.md`** — read it first; it outranks the pipeline data-flow order. The named quality bars are `~/orionfold/books/` (narrative/docs) and the Arena demo `~/ainative-business.github.io/` (static demo); the core requirement is derive-from-and-verify-against the live product (no drift, no fake). Runbook: `_ASSETS/flow/README.md` + `_ASSETS/README.md`. **Data-flow order (NOT priority order):** `features-catalog -> journeys -> seed -> screenshots -> api -> docs -> stats -> demo -> articles`. Full design: `~/.claude/plans/review-the-current-assets-expressive-bird.md`. Memory: [[assets-golden-source-workstream]], [[assets-supervisor-checks-existence-not-behavior]], [[assets-symlink-preserve-symlinks]], [[assets-website-publish-contract]].
  - **🌐 WEBSITE PUBLISH CONTRACT (NEW 2026-07-09, standing law) — `docs/superpowers/specs/2026-07-09-relay-assets-website-publish-contract-design.md` + pinned atop `_ASSETS/README.md`.** docs/api/demo/**memos** (renamed from `articles`, route `/relay/memos/`) are single-source, copy-verbatim **marketing/CTA surfaces** published to **orionfold.com** (`~/orionfold/website/` = PUBLISH TARGET; `ainative-business.github.io` = REFERENCE bar only, NOT a target). One-direction: Relay never writes the website, website never writes `_ASSETS`, Relay reads website read-only to prepare website-ready assets; fidelity enforced at source (vendored DS + drift-check, fails closed). Homes: demo→`public/relay/demo/` (Rail A bundle), memos/docs/api→content collections + `/relay/{memos,docs,api}/` (Rail B). Per-asset conversion (no stacking): **demo=sticky buy strip** (Relay builds into bundle, `live`-gated, never hardcode a live buy state), **memos=interstitial+footer**, **docs/api=footer only**. Website-side work requested via staged `_RELAY` handoffs (A=schemas+CTA hooks, B=per-asset publish), authored when the Relay gate is green, **operator-posted** to `orionfold-website/_RELAY.md` (never commit/push the strategy repo). **This spec shapes the target; it does NOT move asset production up the priority ladder** (enforcement → docs/demo false-greens → consolidation → memos). T1 enforcement + T2 docs + T2 demo (Option C slice) are now DONE; next along the ladder is T3 consolidation → T5 memos, plus the demo fan-out (LIVE #7).
  - **⚠️ "supervisor-green" ≠ Delivered.** The strict supervisor only checks file EXISTENCE, never product-truth — but the `assets-flow` gate now EXECUTES verifiers (T1 done). **Delivered** = built AND behaviorally verified; **Partial** = built but only structurally verified / missing its enforcement gate; **Planned** = spec/stub only. Verified against disk 2026-07-09. Priority order now = **T3 → T5(memos) → T6 (+ demo fan-out, LIVE #7)**:
    | Deliverable | Status | Priority |
    |---|---|---|
    | **T1 Enforcement** — `assets-flow` skill + `supervise-assets.mjs --run-validators` RUN verifiers + gate on exit code; GREEN/AMBER/RED + `fullyVerified` | **Delivered** | done (2026-07-09) |
    | Catalog drift-guard (`{derive,verify}-feature-catalog.mjs` + `pack-aliases.json`, fails CLOSED) — now EXECUTED by the gate | Delivered | done |
    | **T2 Docs / 9 guides** — per-claim accuracy pass done; 8 product-truth defects repaired (nav IA, workflow state vocab, Cost & Usage label, WD controls); `verify-user-guides.mjs` still green 9/9 | **Delivered** | done (2026-07-09) |
    | **T2 Demo** — Option C vertical slice (Support Row Triage lane) BUILT + behaviorally verified (gate GREEN, both supervisor validators pass); captured real UI + DOM-driving boot.js (hydration wall → [[assets-demo-hydration-wall]]); GitHub-Pages target, `next build` capture | **Delivered (slice)** | fan-out next |
    | Seed / Screenshots (88 PNGs) / API / Stats — materially in sync (route count+version match live) | Delivered | verify-not-rebuild |
    | Journeys + coverage-matrix (the coverage contract) | Delivered | stable |
    | **T3 Consolidation** — deprecate `.refresh-pipeline/` + marketing-screenshot scripts; author remaining 5 `assets-` skills (`assets-flow` DONE in T1) | Planned | **P1** |
    | **T5 Memos** (was Articles / Field Notes) — designed (spec committed `ea79474a`); renamed per publish contract; stub README only | Planned | **P2** |
    | **T6 Book** — compile 10–20 articles (Spark→DGX path), joint operator+CC | Planned | deferred |
  - **Why articles are P2, not higher (operator-aligned 2026-07-09):** a built-but-wrong artifact (docs/demo false-greens) is more dangerous than a missing one, so fix the false-greens + stand up enforcement first; T2's mine-metrics/claim-verify discipline de-risks T5's hardest part. The false-green vertical-slice article (article №01) is unusually cheap (this session is its raw material) so it MAY be done opportunistically alongside T2 without displacing enforcement work.
  - **T5 Memos = DESIGN APPROVED, spec committed (parked at P2):** `docs/superpowers/specs/2026-07-09-relay-field-notes-design.md` (committed `ea79474a`) — the deliverable-level spec (lifecycle, vendored DS, verifiers). **Now sits UNDER the publish contract:** renamed Field Notes/articles → **Memos** (`/relay/memos/`); publish target confirmed = orionfold.com; conversion = interstitial+footer, no sticky. At build time reconcile the spec's naming (`_ASSETS/articles/`→`_ASSETS/memos/`, bar `ainative.business/field-notes/` stays a *style* reference not a target). Single source of truth, website copies verbatim, DS vendored + drift-check. 11-stage lifecycle + `assets-field-notes` (rename→`assets-memos`?) skill (draft/verify/publish, gates on exit code). Memo №01 = "The False Green" (this session's story). Memory [[relay-field-notes-pipeline-design]], [[assets-website-publish-contract]]. Next when it reaches the queue: `/handoff`→plan→build the vertical slice.
  - **LIVE #7 — Demo Option C DELIVERED (slice) 2026-07-09** — full detail in top marker + "Recently shipped" + [[assets-demo-hydration-wall]]. **Fan-out phase is the remaining demo work:** the other 3 state machines (Row Trigger, Web Publish, License/Pack update) + the ~30 routes beyond the 5 slice routes + `deploy-relay-demo.mjs` (still a stub). Reuse the built pipeline verbatim (capture→derive→build→verify, all in `_ASSETS/demo/scripts/`); each new machine adds a `machine`-style config to `derive-fixtures.mjs` + a DOM-driving lane + a coach script + gate assertions. Landmines codified in `_ASSETS/demo/README.md`.
  - **LIVE #8 — Consolidation + `assets-` skill family.** `assets-flow` (the executing gate) is BUILT in T1. Remaining: deprecate `.refresh-pipeline/`, `scripts/marketing-screenshots.mjs`+`marketing-hero-popup.mjs` (absorb hero targets into `capture-targets.json`), orphaned `docs/features|journeys|use-cases|index.md|manifest.json` (keep `RELEASING.md`/`trust/`/`codex-browser-runbook.md`/`plugin-security.md`), root `screengrabs/` + `output/marketing-screenshots/`. Author remaining 5 skills: `assets-catalog`, `assets-capture`, `assets-trackers`, `assets-narrative`, `assets-demo`; each with a book-updater-style fail-loud guard. Add `npm run demo:refresh` + structural-fingerprint drift-guard.
  - **DEFERRED (operator, 2026-07-09) — claim-attribution gate for docs.** The per-claim accuracy pass (LIVE #10) is DONE; the operator explicitly deferred extending `verify-user-guides.mjs` toward claim-attribution (currently banned-phrase/section/word-count/image-existence only, so a future guide edit could reintroduce a wrong product-truth claim and still pass). Pick up when the docs corpus next changes or T3 consolidation touches the verifiers.
  - **LIVE #9 — Articles (T6) — JOINT OPERATOR+CC BRAINSTORM, not autonomous generation.** Action = open a brainstorming session with the operator; do NOT batch-generate articles. Both gates now lifted (docs + demo slice verified-green).
  - **🔨 Standing authorization (operator, 2026-07-09):** THROWAWAY-AND-REBUILD any part of `_ASSETS` to meet/exceed the quality/polish/taste bar. Overrides the "prefer small edits / preserve prior work" default for this corpus. Pinned in README origin block; memory [[assets-throwaway-and-rebuild-authorized]].
  - **Advisory:** catalog verify (gh-enabled) surfaces 5 shipped-issue-gap WARNINGS (Marketing line 0.33.0, funnel band 0.34.0, Web Publisher 0.35.0 not cited by version). Not blocking — a grooming signal for `features-catalog.md`.
- **P0 strategy-doc loose end:** `_SPECS/cards-design-system.md` exists through the `_SPECS` symlink as an untracked strategy-repo file (`relay/_SPECS/cards-design-system.md`). Do not commit/push the strategy repo unless the operator explicitly asks.
- **P0 Codex browser runbook is authoritative.** Use `docs/codex-browser-runbook.md`: in-app Browser/Browser plugin first for unauthenticated localhost/file/public pages; Codex Chrome extension for signed-in/profile state; Computer Use for GUI-only desktop flows; Chrome DevTools MCP only for isolated CDP debugging. Do not treat `open -a "Google Chrome" <url>` as sufficient verification.
- **P0 Codex handoff behavior:** `.codex/hooks.json` intentionally has no `Stop` handoff nudge. Use auto compact or operator-initiated `handoff`; do not re-enable the Stop hook unless asked.
- **P0 Codex frontend-skill behavior:** stale Stagent-era frontend design skills were removed from `.agents/skills` and `~/.codex/skills`. Do not reinstall them. Build any new Relay frontend skill later from current online research, Orionfold design-system docs, and project best practices.
- **Packs Publish:** READ trio + R5 + R4-mechanism are built/released in 0.35.0. R4 CUT remains deferred until the tarball size trigger and open decision #2 (most-installed boundary + Website coordination) are answered. R6/R7 community SEND loop stays last, behind TDR-039 substrate.
- **`pack-depth-next-wave` arc:** Video Creator still needs a clean synthetic reference model before build. Retail Investor remains open: value-heatmap + radar, prosumer-first ICP.
- **`_IDEAS/packs-robustify.md`:** R1+R3+R5 built. Next gate tranche: R2 install-time cross-pack collision check, then R7 integration/`joinKeys`, R4 provenanceOf API, R8 dependsOn when it earns weight.
- **Operator-guided feedback tasks (UNGROOMED, source `output/operator-walkthrough-feedback-2026-07-07.md`):** Web-pack preview/gallery polish remains open after the page-registry pass.

Standing candidates (LOW / reactive):
- Card-system follow-up: audit residual detail/table/dashboard cards that still use local raw-badge/status/action layouts, but the main card grids now use the shared footer.
- F5 follow-up: lift dense analytics dashboards `costs/cost-dashboard.tsx` + `apps/ledger-hero-panel.tsx` to the card recipe.
- Not-filed backlog `fix-pricing-bundled-stale-coldstart.md` + R2-4 `create_trigger` `appId` gap.
- #29 retry-with-backoff hardening; held-issue retests (#5/#6/#11/#12).
- Other staging R-runs (one R-run per session; harness ready).
- `chore-deprecated-transitive-deps` (P3); stale `pdfjs-dist` in `serverExternalPackages` (#10).

### Staging harness — ready, full loop proven end-to-end
- `relay-staging` · `staging-cli-run` · `staging-browser-smoke` · `staging-evaluate` are available. Driver-mix + headed-browser default live in the `staging-browser-smoke` skill.
- Re-run cadence: one R-run per session. `scripts/staging/browser-capture.mjs` is the headed PNG helper. `file://` mirror is per-build: `npm run build && node scripts/build-prebuilt-artifact.mjs` before verify.
- Constraints: work on `main`; `_SPECS`/`_IDEAS` edit-only; paid-frontier OK'd for agent steps.

## Known caveats

- **Codex desktop browser caveat:** use the in-app Browser/Browser plugin for ordinary localhost verification. Browser DOM snapshots may fail with an `incrementalAriaSnapshot` runtime mismatch; if so, stay in the in-app Browser and use targeted page evaluation/screenshots per the browser skill rather than falling back to normal Chrome.
- **apiVersion window:** bump `CURRENT_PLUGIN_API_VERSION` + previous-MINOR literal + 3 plugin example YAMLs only on a MINOR release. PATCH does not bump it.
- **The npx prod smoke (`scripts/npx-prod-smoke.mjs`) encodes API contracts that bundled fixes may change** and runs only at release. Before tagging, grep it for endpoints a bundled fix touches.
- **Pack `changelog:` map feeds recap surfaces.** Add a line with every pack version bump; template tests require it for Agency Pro.
- **Pack `price` is `string | {list, intro?, note?}`** behind `packPrice()`; render sites never branch on raw shape.
- **Automation blueprint vars MUST be auto-fillable.** Row-insert required vars need a same-named column or `{{row.<col>}}` default; scheduled blueprint required vars need defaults because app schedules dispatch with no human-supplied variables. `tables.listRows().data` is JSON string; parse before feeding `buildVariables`.
- **docs/index.md + docs/features|journeys|use-cases are gitignored** generated corpus. Public docs = README + SECURITY.md + docs/trust/ + docs/RELEASING.md + docs/plugin-security.md.
- **Pre-existing test failures (NOT regressions), 8:** `router.test.ts` (6), settings validator (1), settings/glance shadow-path (1); `src/__tests__/e2e/blueprint.test.ts` is environmental; `agency-pro-update.test.ts` can flake in full parallel but passes isolation.
- **`next` pinned exactly 16.2.4**; Next 16 emits `.next/node_modules` symlinks; artifact ships manifest + CLI relinks.
- **Blueprint/profile content must pass Zod schema**; registry skips invalid files with console.warn, causing "Blueprint not found" later.
- **Settings mobile overflow caveat:** 2026-07-08 card-system smoke found `/settings#settings-permissions` overflows at 390px because unrelated settings channel/provider rows and telemetry rail cells extend off-canvas. Preset cards were not the source. Track separately if mobile settings polish resumes.
- **`_ASSETS` seed reset caveat:** During the 2026-07-09 screenshot pass, rerunning `seed-relay-demo.mjs --reset-demo` on the already-used seeded DB hit a SQLite foreign-key constraint in `resetDemo()`. A non-reset seed upsert successfully applied the corrected settings and verifier stayed green. Prefer a fresh isolated data dir for future full reset tests, or fix reset deletion order before relying on reset against a reused DB.

## Anchors

- Strategy repo = read/write only unless explicitly instructed; commit/push/merge there only on operator request.
- **Two websites:** `~/orionfold/website/` (orionfold.com) = the PUBLISH TARGET for `_ASSETS`; `~/ainative-business.github.io/` = REFERENCE/quality bar only (never a publish target). Both read-only from Relay; Relay writes neither. See the publish contract atop `_ASSETS/README.md`.
- Work directly on `main`; no worktrees/branches unless operator asks.
- npm publishing uses OIDC via annotated `vX.Y.Z` tag and GitHub Actions. Every release attaches CycloneDX SBOM.
- Runtime-registry-adjacent changes require a real `npm run dev` smoke.
- gh issue/label writes are allowlisted.
- Check git history for prior art; verify field reports before fixing.

## Recently shipped / groomed

**T2 Demo Option C vertical slice (BUILT + behaviorally verified 2026-07-09, strategy-owned/untracked):** Rebuilt `_ASSETS/demo/` as a static, GitHub-Pages-hostable "near-real app" demo of the **real captured Relay UI**. 4-command pipeline in `_ASSETS/demo/scripts/`: `derive-fixtures.mjs` (seed→API-shape fixtures, 4 tests) → `capture-relay-demo.mjs` (Playwright crawl of 5 routes — `/`,`/workflows`,`/tasks`,`/monitor`,`/inbox` — against a seeded `next build && next start`, boot-then-seed order) → `build-relay-demo.mjs` (stitch captured HTML + boot.js + fixtures → `dist/relay/demo/**`) → `verify-relay-demo.mjs` (headless behavioral GATE, exit-code contract, green). `boot.js` (707 lines, 8 tests): network+EventSource shim in the real API/SSE wire shapes + Support-Row-Triage state machine + guidance (DEMO ribbon, live-gated buy strip, step-numbered coach callout, glow-guide ring) + Arena-style streaming activity ticker (token replay, no LLM call). Supervisor already wired — `demo` stage `ok:true`, both validators pass; corpus AMBER only on the unrelated seed validator. Full cold e2e green; 12/12 unit tests; screenshots `output/demo-verify/`. **Key finding → [[assets-demo-hydration-wall]]:** Relay's `force-dynamic` pages can't statically re-hydrate (RSC flight needs a live `next start`), so on the GitHub-Pages target React never attaches and native onClick is inert — the demo therefore DOM-drives interactions via boot.js (structure stays 100% captured / can't drift; only click→state wiring is synthetic). Also: capture must use `next build` not `next dev` (dev throws "Unexpected token '<'" offline); run scripts from repo root + `--preserve-symlinks` (symlink realpath breaks playwright/deps); route URLs need trailing slashes (relative asset resolution). Spec/plan `dbf46722`/`36c498f6`; `_ASSETS` gitignored → operator commits strategy-side. Fan-out (other 3 machines + ~30 routes) = LIVE #7.

**T2 Docs — per-claim accuracy pass (DELIVERED 2026-07-09, strategy-owned/untracked):** The 9 `_ASSETS/docs/` user guides were structurally green (`verify-user-guides.mjs`) but never checked against product-truth — exactly the operator's "green ≠ behaviorally verified" gap. Verified every claim: catalog-restating claims inherit the drift-guarded `features-catalog.md`; beyond-catalog claims (nav labels, state vocab, control names, button strings) got fresh source reads via 3 parallel Explore agents, re-verified against source before asserting in shipped docs. Repaired 8 real defects: (1) guide 01's "sidebar" nav list was substantially wrong — real IA is a two-tier top app-bar (`src/components/shell/nav-items.ts:98-104`), rewrote to real tier-1 (Home/Packs/Compose/Data/Observe) + tier-2 enumerations + Settings-is-a-gear; (2) guide 05 workflow state "stale"→`stalled` + added `paused` (`execution-status.ts` WorkflowEffectiveStatus); (3) guide 05 pattern-surface claim tightened (all 6 at workflow level; blueprints cover sequence/planner-executor/checkpoint per `blueprints/types.ts:38`); (4-6) "Costs"→real nav label "Cost & Usage" in 03/07/09; (7) guide 08 Web Designer control labels tightened to real fields (`static-site-settings.ts`); (8) guide 02 frontmatter screenshot_targets realigned to used figures. Repairs surgical, NOT find-replace (kept "stale" adjective in 4 correct sentences). `verify-user-guides.mjs` re-run green 9/9. Verified everything ACCURATE that the agents said was (WD 7 controls, Schemas-under-Data, Run/Re-run/Stop). Audit at `scratchpad/claims-audit.md`; memory [[assets-docs-claim-verify-beyond-catalog]]. Claim-attribution verifier extension DEFERRED by operator (see above).

**T1 Enforcement — the `assets-flow` executing gate (BUILT + verified 2026-07-09):** Turned the `_ASSETS` supervisor from file-existence-only into a behavioral gate. `flow/scripts/supervise-assets.mjs` gains `--run-validators` (opt-in; bare/`--strict` stay existence-only for back-compat) that spawns each stage's verifier with `node --preserve-symlinks --preserve-symlinks-main` and folds the exit code into failures. Manifest validators became `{script, args?, requires?}` with `{repoRoot}/{dataDir}/{demoDist}` token expansion; the orphaned demo verifier is wired (static pass gates always, behavioral pass `requires:["demoDist"]`); seed `requires:["dataDir"]`. New three-state report — `ok` + `fullyVerified` + a `verification` block + a GREEN/AMBER/RED stderr banner — so a pass with skipped gates reads AMBER, never silently green. Added a `node --test` unit test (`supervise-assets.test.mjs`, 4 cases), two-mode `flow/README.md`, and a new Relay-local (gitignored) `.claude/skills/assets-flow/SKILL.md`. The gate immediately caught a real false-red (docs verifier rejects `--repo-root`) → fixed + pinned as a lesson. Corpus resting state = AMBER (6 pass; seed + demo-behavioral skip pending live infra). Relay-side commit `ea79474a` (Memos spec + `.gitignore` `_ASSETS` + Codex ledger); `_ASSETS` script edits committed in the strategy repo by the operator. Memory [[assets-flow-executing-supervisor-built]], [[assets-verifier-args-must-match-parseargs]].

**`_ASSETS` marketing pipeline — build + review (2026-07-09, strategy-owned/untracked):** Built the corpus + supervisor scaffold, seeded isolated workspace, captured 88 retina screenshots (2× default), drafted 9 narrative book-guide chapters, refreshed stats/positioning, and built the static demo *foundation* — all strict-supervisor green. Then a first-principles review against the operator's two bars diagnosed the demo as a from-scratch mock (not the Arena pattern) and "always in sync" as unenforced; **built two gates this session:** catalog drift-guard (`_ASSETS/catalog/scripts/{derive,verify}-feature-catalog.mjs` + `pack-aliases.json`, fails CLOSED, wired into manifest + strict supervisor) and a Playwright behavioral demo verifier (`verify-relay-demo.mjs`, proven RED on the mock). Detail in git + memory ([[assets-golden-source-workstream]] et al.). Live continuity is the P0 `_ASSETS` item above.

**Card system status toolbar pass (BUILT 2026-07-08, current HEAD):** Added `src/components/shared/card-status-toolbar.tsx` as the shared card footer/status/action primitive. Migrated tasks, workflow Kanban/list cards, blueprints, agents/profiles, presets, projects, schedules, apps, packs, starter templates, and last-run cards to the shared toolbar. Default/ready cards now use the Projects reference wash (`bg-status-running/8 border-t-status-running/15`; dark visual reference `#0d2229`) instead of muted gray, so Blueprints, Agents, Presets, and Projects scan consistently in dark theme; create actions use the app-wide `Plus` icon. Release gate fix: Web Publisher taxonomy now includes `web_pages` and `web_sections.pageSlug`, regenerating `taxonomy.json`. `_SPECS/cards-design-system.md` was updated through the strategy symlink but remains outside Relay commit scope unless explicitly requested. Verification: `npx tsc --noEmit`, `node scripts/check-pack-taxonomy.mjs`, focused taxonomy Vitest (2 files / 24 tests), focused card suite (3 files / 17 tests after final tweaks; broader 6 files / 32 tests earlier in the pass), `git diff --check`, and in-app Browser checks on `/blueprints`, `/agents`, `/presets`, `/tasks`, `/projects`, `/schedules`, `/apps`, and `/packs` with no visible runtime errors.

**Card system neutral-surface pivot (BUILT 2026-07-08, commit 04aaeb6e):** Shared typed cards now keep neutral light/dark card bodies while preserving type color in border accents, watermarks, pills, icons, and footer/status-toolbar washes. Watermarks are proportional and padded top-right; duplicated left-of-title identity icon wells were removed from watermarked card families. Task/workflow kanban cards now show icon-backed lifecycle chips (`Waiting`, `Running`, `Completed`, `Failed`, `Stalled`) and use `Run` vs `Re-run` consistently; workflow list/detail status badges were partially aligned. Verification: `npx tsc --noEmit`, focused card/workflow/task vitest suite (5 files / 29 tests), `git diff --check`, and in-app Browser checks on `/tasks` + `/agents` with no console errors.

**Workflow execution state + Run/Stop controls (BUILT 2026-07-08, current branch before card pass):** Added effective workflow execution state so persisted `active` rows with live child tasks show `running`/Stop, approval waits show `waiting`, and stale active rows show `stalled` rather than implying live work. Workflow cards/list/Kanban/detail expose explicit Run/Re-run/Stop actions; card clicks navigate only.

Older resolved session detail is durable in git history (0.36.0 release, Web Publisher page registry, settings rail simplification, primary container standardization, pack-first IA cleanup, privacy cleanup, and Packs Publish P1/R5/R4 mechanism). Keep HANDOFF focused on live next actions.
