# Relay — HANDOFF

_Last updated: 2026-07-09 (pt: **T5 tail DELIVERED — vendored `_design-system/` snapshot (4 contract files) + `check-design-system-drift.mjs`, the last unbuilt T5 piece. Wired as a preflight into `verify-all-memos.mjs`; fails CLOSED on drift, SKIPs when the reference repo is absent; all 4 drift classes proven RED→GREEN behaviorally; full gate 5/5 green, supervisor AMBER (7 pass). T5 now fully built — only operator editorial review of the 5 draft-1 memos remains. Full narrative + design rationale in "Recently shipped". All under the `_ASSETS`→strategy symlink → operator commits strategy-side (Relay tree clean). Prior detail (T5 draft-1, T3, T2, T1, Demo fan-out LIVE #7) in git.)_

## ▶️ NEXT SESSION — Post-release backlog

- **P0 NEXT — Web Publisher preview/gallery polish.** The page registry is built; remaining operator-guided feedback is to keep web-pack previews inside Relay chrome with a separate raw-view action, make blank gallery thumbnails meaningful or omit them, and normalize section-card click behavior.
- **P0 NEXT — `_ASSETS` golden-source marketing pipeline (multi-session).** Strategy-owned corpus via repo symlink. **⭐ Overriding guidance + verbatim operator origin instruction now live at the TOP of `_ASSETS/README.md`** — read it first; it outranks the pipeline data-flow order. The named quality bars are `~/orionfold/books/` (narrative/docs) and the Arena demo `~/ainative-business.github.io/` (static demo); the core requirement is derive-from-and-verify-against the live product (no drift, no fake). Runbook: `_ASSETS/flow/README.md` + `_ASSETS/README.md`. **Data-flow order (NOT priority order):** `features-catalog -> journeys -> seed -> screenshots -> api -> docs -> stats -> demo -> articles`. Full design: `~/.claude/plans/review-the-current-assets-expressive-bird.md`. Memory: [[assets-golden-source-workstream]], [[assets-supervisor-checks-existence-not-behavior]], [[assets-symlink-preserve-symlinks]], [[assets-website-publish-contract]].
  - **🌐 WEBSITE PUBLISH CONTRACT (NEW 2026-07-09, standing law) — `docs/superpowers/specs/2026-07-09-relay-assets-website-publish-contract-design.md` + pinned atop `_ASSETS/README.md`.** docs/api/demo/**memos** (renamed from `articles`, route `/relay/memos/`) are single-source, copy-verbatim **marketing/CTA surfaces** published to **orionfold.com** (`~/orionfold/website/` = PUBLISH TARGET; `ainative-business.github.io` = REFERENCE bar only, NOT a target). One-direction: Relay never writes the website, website never writes `_ASSETS`, Relay reads website read-only to prepare website-ready assets; fidelity enforced at source (vendored DS + drift-check, fails closed). Homes: demo→`public/relay/demo/` (Rail A bundle), memos/docs/api→content collections + `/relay/{memos,docs,api}/` (Rail B). Per-asset conversion (no stacking): **demo=sticky buy strip** (Relay builds into bundle, `live`-gated, never hardcode a live buy state), **memos=interstitial+footer**, **docs/api=footer only**. Website-side work requested via staged `_RELAY` handoffs (A=schemas+CTA hooks, B=per-asset publish), authored when the Relay gate is green, **operator-posted** to `orionfold-website/_RELAY.md` (never commit/push the strategy repo). **This spec shapes the target; it does NOT move asset production up the priority ladder** (enforcement → docs/demo false-greens → consolidation → memos). T1 enforcement + T2 docs + T2 demo (Option C slice) + **T3 consolidation** are now DONE; next along the ladder is T5 memos, plus the demo fan-out (LIVE #7) + `screengrabs/` cleanup (LIVE #8).
  - **🌐 LIVE publish handoff — `_RELAY` #15 + #16 + #17 (2026-07-09).** #15 (`Relay→Website`): docs+api+demo pickup under a new **`/relay/` sub-nav** (Demo · Docs · API · **Memos reserved**). #16 (`Website→Relay`): **docs LANDED** (committed, unpushed, operator-gated); **api HELD** (not prose-green — the 8 API chapters have placeholder prose; re-flag green after filling); demo/memos pending. **#17 (`Relay→Website`, DRAFTED — operator must post): MEMOS gate-green, fills the reserved slot.** 5-memo "Relay Packs" series → Rail B `src/content/memos/` + `/relay/memos/`; frontmatter schema given (mirrors `relayDocs` + `series`/`signature`/`tags`); SVG tokens **verified already defined in website `src/styles/global.css`** (memos inherit orionfold teal — no new tokens); conversion = interstitial+footer (no sticky); Stage A (collection schema + route) content-independent, can start now; memos are `stage:draft` (operator editorial review pending) so publish-gating on `status:published` is website's call. **Entry written to `_RELAY.md` (uncommitted) — operator commits/pushes the strategy repo.** Draft also at `scratchpad/relay-17-memos-pickup.md`.
    - **⚠️ api NOT green (from #16):** if a future session re-touches `_ASSETS/api/`, the 8 API chapters need real prose (currently placeholder) before re-flagging api publish-ready to the website.
  - **⏳ DARK DEMO — BLOCKED on inbound `_RELAY` from website (do NOT scope yet).** The published demo is **light-theme only** (all 6 captured routes frozen `data-theme="light"`; Option C static HTML has no live theme provider; `dark:` Tailwind classes are inert). Operator decision: **A (publish light-only) for THIS release** — no light-only note added to #15 per operator. Website is building a **themed image component that swaps light/dark screenshots at runtime**; it will send a `_RELAY` back. That component cleanly addresses the *screenshots* (static images) but **may or may not help the demo** (the demo is captured interactive DOM, not a swappable image). **Wait for website's reply before scoping any dark-demo capture pass** — do not start LIVE #7 dark work speculatively.
  - **⚠️ "supervisor-green" ≠ Delivered.** The strict supervisor only checks file EXISTENCE, never product-truth — but the `assets-flow` gate now EXECUTES verifiers (T1 done). **Delivered** = built AND behaviorally verified; **Partial** = built but only structurally verified / missing its enforcement gate; **Planned** = spec/stub only. Verified against disk 2026-07-09. Priority order now = **T5 FULLY DONE (5 memos gate-green awaiting operator editorial review; tail DELIVERED — vendored DS + drift-check) → demo fan-out LIVE #7 (2 remaining machines) → T6 → `screengrabs/` cleanup LIVE #8**:
    | Deliverable | Status | Priority |
    |---|---|---|
    | **T1 Enforcement** — `assets-flow` skill + `supervise-assets.mjs --run-validators` RUN verifiers + gate on exit code; GREEN/AMBER/RED + `fullyVerified` | **Delivered** | done (2026-07-09) |
    | Catalog drift-guard (`{derive,verify}-feature-catalog.mjs` + `pack-aliases.json`, fails CLOSED) — now EXECUTED by the gate | Delivered | done |
    | **T2 Docs / 9 guides** — per-claim accuracy pass done; 8 product-truth defects repaired (nav IA, workflow state vocab, Cost & Usage label, WD controls); `verify-user-guides.mjs` still green 9/9 | **Delivered** | done (2026-07-09) |
    | **T2 Demo** — Option C: 2 lanes now (Support Row Triage + **Web Publish**) on a keyed `machines{}` template; gate GREEN both lanes, supervisor demo stage both verify passes exit 0; real UI + DOM-driving boot.js (hydration wall → [[assets-demo-hydration-wall]]) | **Delivered (2 lanes)** | 2 more machines |
    | Seed / Screenshots (92 PNGs, +hero) / API / Stats — materially in sync (route count+version match live) | Delivered | verify-not-rebuild |
    | Journeys + coverage-matrix (the coverage contract) | Delivered | stable |
    | **T3 Consolidation** — 5 `assets-*` per-stage skills authored; deprecated marketing-screenshot scripts (`1c83e167`, hero → `home-approval-hitl` target); supervisor realpath false-red fixed ([[assets-supervisor-spawns-via-realpath]]). `screengrabs/` cleanup deferred (live skill refs). [[assets-t3-consolidation-built]] | **Delivered** | done (2026-07-09) |
    | **T5 Memos** (was Articles / Field Notes) — DRAFT-1 SERIES COMPLETE (5 memos, all gate-green), operator editorial review pending. Operator opened a **Relay Packs** series (operator BRIEF override, editorial angle operator-owned per spec §1). `articles/`→`memos/` renamed (`git mv`). **Pillar:** why-relay-packs (skills/MCP/plugin/GPT/framework comparison). **Domain memos:** web-designer-pack (table→page you own), **agency-bundle** (free spine + 2 industry sleeves via `relay-agency-cre`/`-nonprofit` bundles), **marketing-line** (2 function packs bound by `source_campaign==utm_campaign` cross-child join), **industry-verticals** (CRE+NP = same queue→trigger→checkpoint→deliverable machine). Each = 8-section article.md + src-cited metrics.json + signature.svg + inline diagram. **All 5 pass `verify-all-memos.mjs`** (number-trace: every prose digit ∈ metrics.json + SVG invariants + scrub CLEAN). SVG craft bar: bold not schematic, color-coded, token-only theme-reactive; all 6 new SVGs previewed Chrome light+dark. **Verify machinery** (`_ASSETS/memos/scripts/`): `verify-svg.mjs` + `verify-article.mjs` + `scrub.mjs` + `new-memo.sh` + **`check-design-system-drift.mjs`** (TAIL DELIVERED); enforcement loop CLOSED (supervisor from REPO ROOT `--run-validators` EXECUTES memo gate w/ drift-check preflight, rests AMBER 7-pass / seed+demo skip on unmet dataDir/demoDist). **Tail DONE:** vendored `_design-system/` (4 contract snapshots) + drift-check re-derives from live REFERENCE repo (`~/ainative-business.github.io`), fails CLOSED on drift, SKIPs when ref absent, wired as memo-gate preflight. T5 fully built; only operator editorial review of the 5 draft-1 memos remains. | **Delivered** | **P1 — operator review of 5 drafts** |
    | **T6 Book** — compile 10–20 articles (Spark→DGX path), joint operator+CC | Planned | deferred |
  - **Why articles are P2, not higher (operator-aligned 2026-07-09):** a built-but-wrong artifact (docs/demo false-greens) is more dangerous than a missing one, so fix the false-greens + stand up enforcement first; T2's mine-metrics/claim-verify discipline de-risks T5's hardest part. The false-green vertical-slice article (article №01) is unusually cheap (this session is its raw material) so it MAY be done opportunistically alongside T2 without displacing enforcement work.
  - **T5 Memos = DESIGN APPROVED, spec committed (parked at P2):** `docs/superpowers/specs/2026-07-09-relay-field-notes-design.md` (committed `ea79474a`) — the deliverable-level spec (lifecycle, vendored DS, verifiers). **Now sits UNDER the publish contract:** renamed Field Notes/articles → **Memos** (`/relay/memos/`); publish target confirmed = orionfold.com; conversion = interstitial+footer, no sticky. At build time reconcile the spec's naming (`_ASSETS/articles/`→`_ASSETS/memos/`, bar `ainative.business/field-notes/` stays a *style* reference not a target). Single source of truth, website copies verbatim, DS vendored + drift-check. 11-stage lifecycle + `assets-field-notes` (rename→`assets-memos`?) skill (draft/verify/publish, gates on exit code). Memo №01 = "The False Green" (this session's story). Memory [[relay-field-notes-pipeline-design]], [[assets-website-publish-contract]]. Next when it reaches the queue: `/handoff`→plan→build the vertical slice.
  - **LIVE #7 — Demo fan-out: machines[] template + Web Publish lane DELIVERED 2026-07-09** — full detail in top marker + "Recently shipped" + [[assets-demo-captured-controls-disabled]]. The pipeline is now a keyed `machines{}` map (proven template); **remaining fan-out work:** the other 2 machines (**Row Trigger**, **License/Pack update**) + the ~30 routes beyond the 6 captured + `deploy-relay-demo.mjs` (still a stub). Recipe for each new machine (all verified this session): add a `derive<Lane>()` builder + entry to the `machines` map in `derive-fixtures.mjs` (project from seed — Row Trigger uses `tables`/row-insert, License uses `apps`+`GET /api/license`/`GET /api/instance/identity` real shapes, NOT the current `/api/instance` stub); add a `resolveFetch` arm + `drive<Lane>` + `coach[]` in `boot.js`; add a route to `resolveAppRoute`/`SLICE_ROUTES` in capture; add a `MACHINES[]` driver entry in verify. Recon wire-shapes for both remaining flows are captured in the session (Row Trigger: `POST /api/tables/{id}/rows` `{rows:[{data}]}`→`{ids,skipped}`, trigger is declarative manifest `row-insert`, async fire-and-forget, no SSE; License/Pack: `GET /api/license` `{licenses:[{valid,expiresAt,...}]}` + `GET /api/instance/identity` `{licenseTag:{kind}}`, `POST /api/packs/{install,update}` 402 `license_required` soft-gate). Landmines in `_ASSETS/demo/README.md` + [[assets-demo-captured-controls-disabled]] (disabled captured controls) + [[assets-demo-hydration-wall]].
  - **LIVE #8 — Consolidation tail (T3 mostly DONE 2026-07-09).** The 5 `assets-*` skills are authored, the marketing scripts + `.refresh-pipeline/` + `output/marketing-screenshots/` are gone, the hero shot is a capture target, the supervisor false-red is fixed ([[assets-t3-consolidation-built]]). **Remaining sweep:** (1) root `screengrabs/` (98 tracked PNGs, ~54MB) — DEFERRED because it's still referenced by live `.agents/skills/supervisor/SKILL.md` + `ainative-app/SKILL.md` + feature specs + seed data; removing it means editing those skill docs FIRST (a distinct blast radius). (2) orphaned `docs/features|journeys|use-cases|index.md|manifest.json` (keep `RELEASING.md`/`trust/`/`codex-browser-runbook.md`/`plugin-security.md`). (3) `npm run demo:refresh` + structural-fingerprint drift-guard.
  - **DEFERRED (operator, 2026-07-09) — claim-attribution gate for docs.** The per-claim accuracy pass (LIVE #10) is DONE; the operator explicitly deferred extending `verify-user-guides.mjs` toward claim-attribution (currently banned-phrase/section/word-count/image-existence only, so a future guide edit could reintroduce a wrong product-truth claim and still pass). Pick up when the docs corpus next changes (T3 consolidation did NOT touch the docs verifier).
  - **LIVE #9 — Book (T6) — JOINT OPERATOR+CC BRAINSTORM, not autonomous generation.** Action = open a brainstorming session with the operator; do NOT batch-generate. The T5 Relay Packs memo series (5 gate-green memos) is now candidate raw material for the compile. Gates lifted (docs + demo slice + memos all verified-green).
  - **🔨 Standing authorization (operator, 2026-07-09):** THROWAWAY-AND-REBUILD any part of `_ASSETS` to meet/exceed the quality/polish/taste bar. Overrides the "prefer small edits / preserve prior work" default for this corpus. Pinned in README origin block; memory [[assets-throwaway-and-rebuild-authorized]].
  - **Advisory:** catalog verify (gh-enabled) surfaces 5 shipped-issue-gap WARNINGS (Marketing line 0.33.0, funnel band 0.34.0, Web Publisher 0.35.0 not cited by version). Not blocking — a grooming signal for `features-catalog.md`.
- **P0 strategy-doc loose end:** `_SPECS/cards-design-system.md` exists through the `_SPECS` symlink as an untracked strategy-repo file (`relay/_SPECS/cards-design-system.md`). Do not commit/push the strategy repo unless the operator explicitly asks.
- **P0 Codex browser runbook is authoritative.** Use `docs/codex-browser-runbook.md`: in-app Browser/Browser plugin first for unauthenticated localhost/file/public pages; Codex Chrome extension for signed-in/profile state; Computer Use for GUI-only desktop flows; Chrome DevTools MCP only for isolated CDP debugging. Do not treat `open -a "Google Chrome" <url>` as sufficient verification.
- **P0 Codex handoff behavior:** `.codex/hooks.json` intentionally has no `Stop` handoff nudge. Use auto compact or operator-initiated `handoff`; do not re-enable the Stop hook unless asked.
- **P0 Codex frontend-skill behavior:** stale Stagent-era frontend design skills were removed from `.agents/skills` and `~/.codex/skills`. Do not reinstall them. Build any new Relay frontend skill later from current online research, Orionfold design-system docs, and project best practices.
- **Packs Publish:** READ trio + R5 + R4-mechanism are built/released in 0.35.0. R4 CUT remains deferred until the tarball size trigger and open decision #2 (most-installed boundary + Website coordination) are answered. R6/R7 community SEND loop stays last, behind TDR-039 substrate.
- **`pack-depth-next-wave` arc:** Video Creator still needs a clean synthetic reference model before build. Retail Investor remains open: value-heatmap + radar, prosumer-first ICP.
- **`_IDEAS/packs-robustify.md`:** R1+R3+R5 built. Next gate tranche: R2 install-time cross-pack collision check, then R7 integration/`joinKeys`, R4 provenanceOf API, R8 dependsOn when it earns weight.
- **Operator-guided feedback tasks (UNGROOMED, source `output/operator-walkthrough-feedback-2026-07-07.md`):** Web-pack preview/gallery polish remains open after the page-registry pass.

Standing candidates (LOW / reactive):
- Card-system follow-up: audit residual detail/table/dashboard cards that still use local raw-badge/status/action layouts, but the main card grids now use the shared footer.
- F5 follow-up: lift dense analytics dashboards `costs/cost-dashboard.tsx` + `apps/ledger-hero-panel.tsx` to the card recipe.
- Not-filed backlog `fix-pricing-bundled-stale-coldstart.md` + R2-4 `create_trigger` `appId` gap.
- #29 retry-with-backoff hardening; held-issue retests (#5/#6/#11/#12).
- Other staging R-runs (one R-run per session; harness ready).
- `chore-deprecated-transitive-deps` (P3); stale `pdfjs-dist` in `serverExternalPackages` (#10).

### Staging harness — ready, full loop proven end-to-end
- `relay-staging` · `staging-cli-run` · `staging-browser-smoke` · `staging-evaluate` are available. Driver-mix + headed-browser default live in the `staging-browser-smoke` skill.
- Re-run cadence: one R-run per session. `scripts/staging/browser-capture.mjs` is the headed PNG helper. `file://` mirror is per-build: `npm run build && node scripts/build-prebuilt-artifact.mjs` before verify.
- Constraints: work on `main`; `_SPECS`/`_IDEAS` edit-only; paid-frontier OK'd for agent steps.

## Known caveats

- **Codex desktop browser caveat:** use the in-app Browser/Browser plugin for ordinary localhost verification. Browser DOM snapshots may fail with an `incrementalAriaSnapshot` runtime mismatch; if so, stay in the in-app Browser and use targeted page evaluation/screenshots per the browser skill rather than falling back to normal Chrome.
- **apiVersion window:** bump `CURRENT_PLUGIN_API_VERSION` + previous-MINOR literal + 3 plugin example YAMLs only on a MINOR release. PATCH does not bump it.
- **The npx prod smoke (`scripts/npx-prod-smoke.mjs`) encodes API contracts that bundled fixes may change** and runs only at release. Before tagging, grep it for endpoints a bundled fix touches.
- **Pack `changelog:` map feeds recap surfaces.** Add a line with every pack version bump; template tests require it for Agency Pro.
- **Pack `price` is `string | {list, intro?, note?}`** behind `packPrice()`; render sites never branch on raw shape.
- **Automation blueprint vars MUST be auto-fillable.** Row-insert required vars need a same-named column or `{{row.<col>}}` default; scheduled blueprint required vars need defaults because app schedules dispatch with no human-supplied variables. `tables.listRows().data` is JSON string; parse before feeding `buildVariables`.
- **docs/index.md + docs/features|journeys|use-cases are gitignored** generated corpus. Public docs = README + SECURITY.md + docs/trust/ + docs/RELEASING.md + docs/plugin-security.md.
- **Pre-existing test failures (NOT regressions), 8:** `router.test.ts` (6), settings validator (1), settings/glance shadow-path (1); `src/__tests__/e2e/blueprint.test.ts` is environmental; `agency-pro-update.test.ts` can flake in full parallel but passes isolation.
- **`next` pinned exactly 16.2.4**; Next 16 emits `.next/node_modules` symlinks; artifact ships manifest + CLI relinks.
- **Blueprint/profile content must pass Zod schema**; registry skips invalid files with console.warn, causing "Blueprint not found" later.
- **Settings mobile overflow caveat:** 2026-07-08 card-system smoke found `/settings#settings-permissions` overflows at 390px because unrelated settings channel/provider rows and telemetry rail cells extend off-canvas. Preset cards were not the source. Track separately if mobile settings polish resumes.
- **`_ASSETS` seed reset caveat:** During the 2026-07-09 screenshot pass, rerunning `seed-relay-demo.mjs --reset-demo` on the already-used seeded DB hit a SQLite foreign-key constraint in `resetDemo()`. A non-reset seed upsert successfully applied the corrected settings and verifier stayed green. Prefer a fresh isolated data dir for future full reset tests, or fix reset deletion order before relying on reset against a reused DB.

## Anchors

- Strategy repo = read/write only unless explicitly instructed; commit/push/merge there only on operator request.
- **Two websites:** `~/orionfold/website/` (orionfold.com) = the PUBLISH TARGET for `_ASSETS`; `~/ainative-business.github.io/` = REFERENCE/quality bar only (never a publish target). Both read-only from Relay; Relay writes neither. See the publish contract atop `_ASSETS/README.md`.
- Work directly on `main`; no worktrees/branches unless operator asks.
- npm publishing uses OIDC via annotated `vX.Y.Z` tag and GitHub Actions. Every release attaches CycloneDX SBOM.
- Runtime-registry-adjacent changes require a real `npm run dev` smoke.
- gh issue/label writes are allowlisted.
- Check git history for prior art; verify field reports before fixing.

## Recently shipped / groomed

**T5 tail — vendored `_design-system/` + drift-check (DELIVERED 2026-07-09, strategy-owned/untracked):** The last unbuilt T5 piece, closing the memos pipeline. **4 vendored contract snapshots** under `_ASSETS/memos/_design-system/`, each with a provenance header (source repo/path + snapshot commit `0b883f0`): `frontmatter.schema.json` (memo FM contract, records where it deliberately narrows the reference `fieldNotes` collection — summary cap 300 vs ref 400, Relay-owned `stage`/`status` lifecycle enums), `svg-invariants.json` (the ~10 rules `verify-svg.mjs` implements, enforced vs deliberately-not-ported split), `explainers.json` (the `:::define/why/deeper/pitfall/math/hardware` directive set + labels), `tokens.json` (the `--svg-accent-*`/`--color-primary` NAME surface + an explicit `_publish_target_divergence` note: `~/orionfold/website` collapsed the token VALUES, so the snapshot pins the richer REFERENCE repo). **`check-design-system-drift.mjs`** mirrors the catalog drift-guard (exported `runChecks` + `isDirectEntrypoint` realpath guard for the `_ASSETS` symlink): re-derives all four from the live REFERENCE repo (`~/ainative-business.github.io` — canonical field-notes DS, NOT the publish target), diffs, **fails CLOSED** on drift, degrades to **SKIP-with-warning (exit 0)** when the ref repo is absent, cross-checks publish-target token fidelity as WARN-only, never auto-syncs. **Wired as a preflight** into `verify-all-memos.mjs` → drift fails the WHOLE memo stage. **PROVEN behaviorally, not asserted:** all 4 drift classes (dropped SVG invariant / dead token / relabeled directive / dropped required FM field) go RED with a precise message and revert GREEN (empty git diff); SKIP + fail-CLOSED paths confirmed by exit code; full memo gate 5/5 green with preflight; supervisor from repo root rests correct AMBER (7 pass / seed+demo skip). Canonical-source picked via a parallel Explore recon that found the field-notes DS lives ONLY in the reference repo (the publish target carries no `field-notes` collection + diverged token values — itself the drift this guard now watches). All under the `_ASSETS`→`strategy/relay/_ASSETS` symlink → operator commits strategy-side (Relay tree clean). Memory [[relay-field-notes-pipeline-design]].

**T5 Memos draft-1 series (5 memos, DELIVERED 2026-07-09, gate-green, awaiting operator editorial review):** `why-relay-packs` pillar + `web-designer-pack`/`agency-bundle`/`marketing-line`/`industry-verticals` domain memos; each 8-section article.md + src-cited metrics.json + signature.svg + inline `fn-diagram`. Verify-before-write (3 Explore agents mined `src/lib/packs/templates/**`) caught 3 structural errors the number-trace gate can't. Full detail in git + [[t5-memos-relay-packs-series]], [[memos-number-trace-misses-structural-claims]].

Older resolved session detail is durable in git + memory: Demo fan-out session 1 (`machines{}` template + Web Publish lane, [[assets-demo-captured-controls-disabled]]), T3 consolidation (`assets-*` skills + marketing-script deprecation `1c83e167`, [[assets-t3-consolidation-built]]), T2 Demo Option C slice (`dbf46722`/`36c498f6`, [[assets-demo-hydration-wall]]), T2 Docs claim-verify ([[assets-docs-claim-verify-beyond-catalog]]), T1 enforcement gate (`ea79474a`, [[assets-flow-executing-supervisor-built]]), `_ASSETS` pipeline build+review ([[assets-golden-source-workstream]]); and 2026-07-08 card-system pivots (`04aaeb6e`), 0.36.0 release, Web Publisher page registry, Packs Publish P1/R5/R4. Keep HANDOFF focused on live next actions.
